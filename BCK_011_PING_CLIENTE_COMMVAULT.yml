---
- hosts: all
  tasks:
  - name: Prueba ping
    win_command: cmd.exe /c ping "{{ cliente }}"
    register: ping
  - debug: var=ping.stdout_lines

  - debug: 
      msg: "RESULTADO EJECUCION: {{ ping.stdout }}"
