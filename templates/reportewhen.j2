<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Reporte {{ comandos }}</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; }
        h1 { color: #2E86C1; }
        pre { background: #F4F6F6; padding: 10px; border: 1px solid #D5DBDB; border-radius: 5px; }
        .host { margin-bottom: 30px; }
    </style>
</head>
<body>
    <h1>Reporte de ejecución</h1>
    <p><strong>Comando ejecutado:</strong> {{ lista_comandos[comandos] }}</p>

    {% for host, lineas in resultados.items() %}
    <div class="host">
        <h2>Host: {{ host }}</h2>
        <pre>
{% for linea in lineas %}
{{ linea }}
{% endfor %}
        </pre>
    </div>
    {% endfor %}
</body>
</html>
